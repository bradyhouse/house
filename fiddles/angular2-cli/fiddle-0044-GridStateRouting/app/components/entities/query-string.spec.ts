import {
  describe,
  ddescribe,
  expect,
  iit,
  it
} from '@angular/core/testing';
import {QueryString} from './query-string';

describe('QueryString', () => {
  it('should create an instance', () => {
    expect(new QueryString()).toBeTruthy();
  });
});
